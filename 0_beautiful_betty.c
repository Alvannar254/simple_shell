#include <stdio.h>

/**
 * main - Entry point of the program
 * This function uses the printf function to print the string
 * followed by a newline to the standard output.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	printf("Hello, World\n");

	return (0);
}

